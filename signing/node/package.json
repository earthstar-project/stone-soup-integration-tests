{
  "name": "node",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "clean": "rm -rf build/*",
    "build.tsc": "tsc",
    "build.esbuild": "node esbuild-node-bundle.js",
    "build": "npm run build.tsc ; npm run build.esbuild",
    "start.tsc": "echo -------------------- STARTING TSC FILE && node build/index.js",
    "start.esbuild": "echo -------------------- STARTING ESBUILD BUNDLE && node build/index.bundle.js",
    "start": "npm run start.tsc ; npm run start.esbuild",
    "everything": "npm run clean && npm ci && npm run build && npm run start ; ls -lah build",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Cinnamon",
  "license": "ISC",
  "dependencies": {
    "stone-soup": "^7.0.2"
  },
  "devDependencies": {
    "@types/node": "^17.0.8",
    "esbuild": "^0.14.10",
    "typescript": "^4.6.0-dev.20220108"
  }
}
